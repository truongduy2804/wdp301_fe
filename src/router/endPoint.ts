const endPoint = {
  HOMEPAGE: "/",
  AUTH: "/auth",
  LOGIN: "/auth?view=login",
  REGISTER: "/auth?view=register",
  FORGOTPASSWORD: "/auth?view=forgotpass",
  
  // Waste platform portals
  ADMIN: "/admin",
  ENTERPRISE: "/enterprise", // Recycling Enterprise portal
  COLLECTOR: "/collector",   // Collector portal
  CITIZEN: "/citizen",       // Citizen portal
};

export default endPoint;
